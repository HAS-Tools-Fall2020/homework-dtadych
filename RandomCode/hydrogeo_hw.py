#%%
# Import the modules we will use
import os
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

#%%
sigma = 
avg = 
size = 
# %%

for j in range(0, 999):
    T = 0
    for i in range(0,11)
        R = numpy.random.Generator.lognormal(mean=avg, sigma=sigma, size=size)
        T = T + R
    V1 = T - 6
    V2 =